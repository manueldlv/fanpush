# Executions

Store the concrete implementation record here.

Each execution should note:

- related `plan_id`
- who executed it
- what files changed
- what validation ran
- whether the result matched the plan
