# Request

- title: Corregir permission denied al correr npm run dev
- requester: matias
- functional_goal: Evitar que `npm run dev` falle por permisos del script local.
- constraints:
  - La solucion tiene que funcionar aunque el archivo no tenga bit ejecutable.
  - Mantener el flujo actual de dev y el fallback de puerto.
- current_status: completed
- scope: safe
