# Changelog

- what_was_asked: Corregir el fallo `Permission denied` al correr `npm run dev`.
- what_was_approved: Ajuste tecnico local y compatible en scripts de arranque.
- what_was_done: `package.json` ahora llama los scripts con `zsh` y se restauraron permisos de ejecucion.
- why_it_matches: El arranque ya no depende del bit ejecutable del archivo y vuelve a funcionar desde `npm`.
